# Managed by go-ispconfig install.
# Written only when the distro nginx.conf does not already include
# sites-enabled; the web module activates vhosts by symlinking there.
include /etc/nginx/sites-enabled/*;
