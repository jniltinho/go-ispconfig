# Reference ISPConfig3 source — never committed
base/

# Local-only validation prints (curated screenshots go in docs/screenshots/)
docs/prints/

# Vagrant images/state and root-level test dumps — never committed.
# Package fixtures live under internal/*/testdata/ or internal/*/golden/ and MUST be tracked.
.vagrant/
*.box
/testdata/
*.sql.gz
*.dump

# Sensitive / local config
config.toml
config.local.toml
*.pem
*.key
credentials*
AGENTS.local.md

# Build artifacts (web/dist/index.html placeholder stays tracked so the
# go:embed of web/dist always compiles from a fresh clone)
bin/
web/dist/*
!web/dist/index.html
go-ispconfig
*.exe
*.test
*.out
coverage.out
tmp/
build/
*.deb
*.rpm
*.tar.gz

# Frontend
frontend/node_modules/

# Databases
*.db
*.db-shm
*.db-wal

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
.agents/scheduled_tasks.lock
.mail.env

# Reference ISPConfig3 source symlink and local agent notes
base
.hermes/
